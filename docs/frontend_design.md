# frontend

This project uses "pnpm" command for dependencies.

node.js for vite: v18.20.8
pnpm version: 10.27.0

```
corepack disable
sudo npm install -g pnpm
pnpm --version

pnpm create vite@5 frontend --template react-ts

cd frontend
pnpm installs
pnpm add react-router-dom

pnpm dev
```

